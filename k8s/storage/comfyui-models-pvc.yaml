apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comfyui-models
  namespace: default
  labels:
    app: comfyui
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-ssd
  resources:
    requests:
      storage: 1000Gi
